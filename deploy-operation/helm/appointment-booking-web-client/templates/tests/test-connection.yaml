apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "appointment-booking-web-client.fullname" . }}-test-connection"
  labels:
    {{- include "appointment-booking-web-client.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "appointment-booking-web-client.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
