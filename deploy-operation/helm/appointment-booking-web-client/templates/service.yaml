apiVersion: v1
kind: Service
metadata:
  name: {{ include "appointment-booking-web-client.fullname" . }}
  labels:
    {{- include "appointment-booking-web-client.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "appointment-booking-web-client.selectorLabels" . | nindent 4 }}
